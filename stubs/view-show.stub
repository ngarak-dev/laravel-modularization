@extends('{{moduleNameLower}}::layouts.module-layout')

@section('title', '{{ __("{{moduleName}} Details") }}')

@section('content')
<div class="container mx-auto py-6">
    <div class="flex justify-between items-center mb-6">
        <h1 class="text-2xl font-bold text-gray-800">{{ __('{{moduleName}} Details') }}</h1>
        <a href="{{ route('{{moduleNameLower}}.index') }}" class="text-gray-600 hover:text-gray-900">
            &larr; {{ __('Back to List') }}
        </a>
    </div>

    <div class="bg-white shadow-md rounded-lg overflow-hidden">
        <div class="p-6">
            <dl class="grid grid-cols-1 gap-x-4 gap-y-6 sm:grid-cols-2">
                <div>
                    <dt class="text-sm font-medium text-gray-500">ID</dt>
                    <dd class="mt-1 text-sm text-gray-900">{{ ${{moduleNameLower}}->id }}</dd>
                </div>
                <div>
                    <dt class="text-sm font-medium text-gray-500">Name</dt>
                    <dd class="mt-1 text-sm text-gray-900">{{ ${{moduleNameLower}}->name }}</dd>
                </div>
                <div>
                    <dt class="text-sm font-medium text-gray-500">Created At</dt>
                    <dd class="mt-1 text-sm text-gray-900">{{ ${{moduleNameLower}}->created_at->format('Y-m-d H:i:s') }}</dd>
                </div>
                <div>
                    <dt class="text-sm font-medium text-gray-500">Updated At</dt>
                    <dd class="mt-1 text-sm text-gray-900">{{ ${{moduleNameLower}}->updated_at->format('Y-m-d H:i:s') }}</dd>
                </div>
            </dl>
        </div>

        <div class="bg-gray-50 px-6 py-4 flex justify-end space-x-3">
            <a href="{{ route('{{moduleNameLower}}.edit', ${{moduleNameLower}}->id) }}" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">
                {{ __('Edit') }}
            </a>
            <form action="{{ route('{{moduleNameLower}}.destroy', ${{moduleNameLower}}->id) }}" method="POST" class="inline" onsubmit="return confirm('Are you sure you want to delete this item?')">
                @csrf
                @method('DELETE')
                <button type="submit" class="bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded">
                    {{ __('Delete') }}
                </button>
            </form>
        </div>
    </div>
</div>
@endsection
